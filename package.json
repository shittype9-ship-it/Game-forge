{
  "name": "gameforge",
  "version": "1.0.0",
  "description": "A full-featured game engine for building 2D and 3D games",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/backend",
    "packages/engine"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  }
}
